%ol.formlist.inline
  %li= f.select :sexe, [['?', 0], ['M', 1], ['F', 2]], {}, {:tabindex => autotab}
  %li= f.text_field :poids,  :size => 4, :tabindex => autotab
  %li= f.text_field :taille, :size => 4, :tabindex => autotab
  %li= f.text_field :pc,     :size => 3, :tabindex => autotab
  %li= f.text_field :apgar1, :size => 3, :tabindex => autotab
  %li= f.text_field :apgar5, :size => 3, :tabindex => autotab
  %li.ten
    - 3.times{f.object.malformation_assignments.build}
    = f.fields_for :malformation_assignments do |malf_form|
      = render 'malformation_assignment', :f => malf_form

  %li.ten
    - 3.times{f.object.pathologie_assignments.build}
    = f.fields_for :pathologie_assignments do |malf_form|
      = render 'pathologie_assignment', :f => malf_form
    -#= f.select :pathologie, [['Non', 0], ['Oui', 1]], {}, {:tabindex => autotab}
  %li= f.link_to_remove "Détruire"
